# 主页 README 使用说明

展示名为 `yixiongli`，GitHub 用户名为 `liiiyiiixiii`。主页仓库必须命名为 `liiiyiiixiii`，完整路径是 `liiiyiiixiii/liiiyiiixiii`。

1. 在该账号下创建 Public 仓库 `liiiyiiixiii`，默认分支使用 `main`。
2. 上传本目录的 `README.md`、`assets/` 和 `.github/workflows/snake.yml`，保留目录结构。
3. 在 Actions 中检查 `Generate contribution snake` 首次运行；必要时点击 Run workflow。成功后会生成 `output` 分支中的亮色、暗色贡献蛇动画。
4. 打开个人主页，检查排版、技术图标、作品链接和活动卡片。

贡献蛇每天自动更新。配置使用仓库内置的 `GITHUB_TOKEN`，无需添加个人令牌。首次工作流成功前，贡献蛇图片尚不存在；本地预览不能验证远端工作流是否成功。

打字动画、徽章与活动统计由第三方服务提供，其可用性取决于服务状态。项目介绍和技术图标保存在仓库内；外部统计不影响作品链接。

页面参考 Leizhidong-creator 的章节结构和技术分类，个人介绍和项目文案已重新编写。未加入奖项、项目分工、手机号或微信号。

贡献动画配置参考：https://github.com/Platane/snk
